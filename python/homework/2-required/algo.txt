This is a test 2.0
# Al-Khwarizmi

Consiglio di copiare e incollare il testo su https://markdownlivepreview.com/ per una lettura più fluida. 


# Do or do not. There is no try

Il problema è relativamente breve, tanto che, facendo a meno delle ultime due funzioni (nel contesto della mia soluzione) si risparmia il 5% di tempo computazionale senza aumentare "l'intricatezza" del codice (la riduzione è costante al 10%, anche su testcase più grandi). 

Per cui, illustrerò entrambe le soluzioni possibili.


## Powerful you have become, the dark side I sense in you

La prima soluzione consiste nel usare lo schema proposto dal problema per suddividere le varie parti della soluzione. La prima funzione `decode_XKCD_tuple` è semplice: si decodifica ciascuna stringa, si riordinano in maniera decrescente i valori, e si prende una "slice" dei primi `k`.

> La funzione `map` è più veloce della "list comprehension" quando non uso una lambda
> `.sort()` dovrebbe essere più veloce di `sorted()` essendo "in-place"
-- > Sarebbe utile evitare di fare una copia con le "slice", ma è l'unica soluzione ragionevole 


Per la decodifica la funzione `decode_value` converte la stringa in una lista di valori (con `xkcd_to_list_of_weights`) e li somma (con `list_of_weights_to_number`)

> Questo per la prima soluzione, per la seconda si usa direttamente `decode_value` per decodificare la stringa "in-place" 


### xkcd_to_list_of_weights

Dato `xkcd = '10010010010100511'`, la strategia più conveniente è quella di partire dal fondo: se partissimo dalla prima cifra, dovremmo contare quanti 0 abbiamo dopo ogni cifra diversa da 0 (nel primo caso, partendo da `'1'`, dobbiamo contare i 2 zeri dopo, aggiungere `'100'` al risultato e spostare l'indice di 2 posizioni). Partendo dall'ultima cifra, bisogna preoccuparsi solo se questa è `'0'` o meno


### list_of_weights_to_number

### decode_value


## You must unlearn what you have learned (Yoda)

Si può usare "decode_value" per decodificare direttamente la stringa, permettendo di saltare alcuni passaggi (sempre basandosi sull'idea precedentemente illustrata)


Citazioni di Yoda perche' mi va anche se non ho mai visto StarWars
